<%- contentFor('body') %>

    <div class="container-fluid px-4">
        <h1 class="mt-4">Employee create</h1>
        <ol class="breadcrumb mb-4">
            <li class="breadcrumb-item active">Employee create</li>
        </ol>
     
        <div class="card mb-4">
            <div class="card-header">
                <i class="fas fa-table me-1"></i>
                Employee create
            </div>
            <div class="card-body">
                <div id="layoutAuthentication">
                    <div id="layoutAuthentication_content">
                        <main>
                            <div class="container">
                                <div class="row justify-content-center">
                                    <div class="col-lg-7">
                                        <div class="card shadow-lg border-0 rounded-lg mt-5">
                                            <div class="card-header"><h3 class="text-center font-weight-light my-4">Create Employee</h3></div>
                                            <div class="card-body">
                                                <% if (success || error || (errorMessages && errorMessages.length > 0)) { %>
                                                    <div>
                                                        <% if (success) { %>
                                                        <div class="alert alert-success <% if (success.length == 0) { %> d-none <% } %>"><%= success %></div>
                                                      <% } %>
                                                      <% if (error) { %>
                                                        <div class="alert alert-danger <% if (error.length == 0) { %> d-none <% } %>"  ><%= error %></div>
                                                      <% } %>
                                                      <% if (errorMessages && errorMessages.length > 0) { %>
                                                        <div class="alert alert-danger">
                                                          <ul>
                                                            <% errorMessages.forEach(function(msg) { %>
                                                              <li><%= msg %></li>
                                                            <% }); %>
                                                          </ul>
                                                        </div>
                                                      <% } %>
                                                    </div>
                                                  <% } %>
                                                  
                                                <form  action="/employee/add-emp" method="post" accept-charset="utf-8">
                                                    <div class="row mb-3">
                                                        <div class="col-md-6">
                                                            <div class="form-floating mb-3 mb-md-0">
                                                                <input class="form-control" value="<%= userData ? userData.firstName : ''  %>" name="firstName" id="firstName" type="text" placeholder="Enter your first name" />
                                                                <label for="firstName">First name</label>
                                                            </div>
                                                        </div>
                                                        <div class="col-md-6">
                                                            <div class="form-floating">
                                                                <input class="form-control" value="<%= userData ? userData.lastName : ''  %>" name="lastName" id="lastName" type="text" placeholder="Enter your last name" />
                                                                <label for="lastName">Last name</label>
                                                            </div>
                                                        </div>
                                                    </div>
                                                    <div class="form-floating mb-3">
                                                        <input class="form-control" name="email" value="<%= userData ? userData.email : ''  %>" id="email" type="email" placeholder="name@example.com" />
                                                        <label for="email">Email address</label>
                                                    </div>
                                                   
                                                    <div class="row mb-3">
                                                        <div class="col-md-6">
                                                            <div class="form-floating mb-3 mb-md-0">
                                                                <input class="form-control" name="password" value="<%= userData ? userData.password : ''  %>" id="password" type="password" placeholder="Create a password" />
                                                                <label for="password">Password</label>
                                                            </div>
                                                        </div>
                                                        <div class="col-md-6">
                                                            <div class="form-floating mb-3 mb-md-0">
                                                                <input class="form-control" name="passwordConfirm" id="passwordConfirm" type="password" placeholder="Confirm password" />
                                                                <label for="passwordConfirm">Confirm Password</label>
                                                            </div>
                                                        </div>
                                                    </div>
                                                    <!-- <div class="row mb-3">
                                                        <div class="col-md-12">
                                                            <div class="form-floating mb-3 mb-md-0">
                                                                <select class="form-control" name="department" id="department">
                                                                    <option value="">Select</option>
                                                                    <% departments.forEach(function(dept)  {
                                                                        %>
                                                                         <option value="<%= dept.id %>" <%= userData && userData.department == dept.id ? 'selected' : '' %> > <%= dept.department_name %></option>
                                                                        <%
                                                                        });
                                                                    %>
                                                                </select>
                                                                <label for="department">Department</label>
                                                            </div>
                                                        </div>
                                                       
                                                    </div> -->
                                                    <div class="mt-4 mb-0">
                                                        <div class="d-grid"><button type="submit" class="btn btn-primary btn-block" >Create Account</button></div>
                                                    </div>
                                                </form>
                                            </div>
                                           
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </main>
                    </div>
                    
                </div>
            </div>
        </div>
    </div>